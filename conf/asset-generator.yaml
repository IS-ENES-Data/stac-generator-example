generator: asset

collection_descriptions:
  root_directory: collection_descriptions/

inputs:
  - name: intake_esm
    uri: https://gitlab.dkrz.de/data-infrastructure-services/intake-esm/-/raw/master/esm-collections/cloud-access/dkrz_cmip6_cloud.json
    object_path_attr: uri

outputs:
  #- name: standard_out
  - name: text_file
    filepath: tests/file-io/assets.txt
  - name: elasticsearch_bulk
    namespace: asset
    connection_kwargs:
      hosts: ["http://0.0.0.0:9200"]
      #hosts: ['http://136.172.60.54:9200']
      #hosts: ['http://136.172.16.122:9200']
      # use_ssl: false
      verify_certs: false
      ssl_show_warn: false
    index:
      name: stac-assets
    cache_max_size: 100
logging:
  level: INFO
